package site.shaobao.app;

public class Config {
    // Base server URL. Change this in one place to switch servers.
    public static final String BASE_URL = "http://127.0.0.1:5000";//输入服务器地址，注意末尾不要加斜杠

    private Config() {
        // prevent instantiation
    }
}
